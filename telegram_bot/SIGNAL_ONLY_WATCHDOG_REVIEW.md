# Signal-Only Watchdog — Production-Path Integration Tests

## Status

**DRAFT — NOT APPROVED FOR PRODUCTION**

This PR presents signal-only control infrastructure with production-path integration tests.

## Authoritative Requirements

1. **Environment Mapping Verification** — Pending
   - The authoritative `SIGNAL_BOT_ENV_MAPPING_VERIFIED` setup and approval is maintained in DigitalOcean deployment configuration, not this repository.
   - This PR implements the gate logic. Activation requires the external environment setup.

2. **Scanner Protection** — Unchanged
   - `scanner_bot.py` is protected and outside this review scope.
   - The watchdog maintains observe-only mode: no restart, no signal, no file writes.

3. **Production Bots** — Not Started
   - No forex, gold, crypto, or NIFTY bots were started during testing.
   - All tests use mock fixtures or real function validation without launching live traders.

4. **DigitalOcean Deployment** — Not Performed
   - No DigitalOcean systems were accessed.
   - No credentials were used.
   - No production environment was modified.

## Implementation

### Activation Gate

```bash
require_verified_env_mapping()
  ↓
Blocks startup and watchdog restart unless:
SIGNAL_BOT_ENV_MAPPING_VERIFIED=APPROVED
```

This function is:
- Defined in `signal_bot_common.sh`
- Called at the entry point of `start_signal_bots.sh` (before any action)
- Called at the restart path of `watchdog_signal_only.sh` (before any restart)
- Not present by default (fail-closed)

### Production-Path Integration Tests

20 tests verify:

**Basic Function Validation (Tests 1–13)**
- Process discovery (absent, single, duplicate)
- Owner validation (UID, username, rejection)
- Prohibited process and file detection
- .env parsing (duplicates, malformed, empty, comments)

**Production Entry-Point Tests (Tests 14–20)**
- Gate blocking without `APPROVED` (startup)
- Gate blocking without `APPROVED` (watchdog)
- Gate allowing with `APPROVED` (startup proceeds)
- Gate allowing with `APPROVED` (watchdog proceeds)
- Scanner observe-only (no action)

## Test Results

All 20 tests pass:
- Syntax validation: ✓ (4 scripts)
- Function validation: ✓ (13 tests)
- Production-path integration: ✓ (7 tests)
- Zero launch on gate block: ✓
- Zero restart on gate block: ✓

## Files

Permitted files in this PR:
- `.github/workflows/signal-bot-validation.yml` — CI configuration
- `telegram_bot/signal_bot_common.sh` — Shared validation library
- `telegram_bot/start_signal_bots.sh` — Bot startup entry point
- `telegram_bot/watchdog_signal_only.sh` — Bot watchdog monitoring
- `telegram_bot/test_scripts_signal_only.sh` — 20 integration tests
- `telegram_bot/SIGNAL_ONLY_WATCHDOG_REVIEW.md` — This documentation

## Explicit Non-Scope

Excluded from this PR:
- `scanner_bot.py` (protected, unchanged)
- Trading bot implementations (forex, gold, crypto, NIFTY)
- Strategy baseline or testing files
- DigitalOcean setup or credentials
- State files, logs, or database
- Production deployment automation

## Next Steps

1. CTO GitHub review of PR
2. Approval of environment mapping configuration
3. Activation in the authoritative DigitalOcean environment
4. Deployment to production (separate authorization)

---

**Generated by Claude Code**  
**Session:** https://claude.ai/code/session_0143YKzSxT6cbqvsyZNUVDtf
